async function renderTechnology(){
 setHeader('Tecnologia & Observabilidade','Saúde dos produtos, endpoints e repositórios — a primeira camada do control plane técnico.');
 root.innerHTML='<div class="empty">Verificando endpoints e GitHub…</div>';
 try{const r=await apiFetch('/api/tech-overview');state.tech=await r.json()}catch{state.tech={items:[]}}
 const rows=state.tech.items||[];
 root.innerHTML=`<div class="grid4">${kpi('Operacionais',rows.filter(x=>x.health==='healthy').length,'checks atuais')}${kpi('Em atenção',rows.filter(x=>x.health==='degraded').length,'latência/HTTP')}${kpi('Indisponíveis',rows.filter(x=>x.health==='down').length,'investigar')}${kpi('Não verificados',rows.filter(x=>x.health==='unknown').length,'sem endpoint')}</div><div class="section-head"><h2>Portfólio técnico</h2><p>GitHub + HTTP health</p></div><section class="panel table-wrap"><table class="table"><thead><tr><th>Venture</th><th>Saúde</th><th>Latência</th><th>Repo</th><th>Branch</th><th>Última atualização</th></tr></thead><tbody>${rows.map(x=>`<tr><td><b>${esc(x.venture)}</b></td><td>${status(x.health)}</td><td>${x.latencyMs?x.latencyMs+' ms':'—'}</td><td class="muted">${esc(x.repo||'—')}</td><td>${esc(x.branch||'—')}</td><td class="muted">${x.repoUpdatedAt?new Date(x.repoUpdatedAt).toLocaleString('pt-BR'):'—'}</td></tr>`).join('')}</tbody></table></section><div class="callout" style="margin-top:16px">Próxima camada: Render + Netlify + OpenStatus para SHA em produção, builds, uptime histórico, SSL, incidentes e alertas.</div>`;
}

async function renderFinance(){
 setHeader('Financeiro','Caixa realizado, custos e resultado operacional — separado de pipeline e projeções.');const snap=await fetchSnapshot();const cash=snap.cash||[];const rev=cash.filter(x=>x.kind==='revenue').reduce((a,x)=>a+Number(x.amount_brl||0),0);const cost=cash.filter(x=>x.kind==='cost').reduce((a,x)=>a+Number(x.amount_brl||0),0);
 root.innerHTML=`<div class="grid3">${kpi('Receita realizada',money(rev),'fontes conectadas')}${kpi('Custos registrados',money(cost),'infra + operação')}${kpi('Resultado operacional',money(rev-cost),rev-cost>=0?'positivo / não consolidado':'negativo / não consolidado')}</div><div class="section-head"><h2>Movimentações</h2><p>${cash.length?'Data Hub':'aguardando conectores'}</p></div><section class="panel table-wrap">${cash.length?`<table class="table"><thead><tr><th>Data</th><th>Venture</th><th>Tipo</th><th>Fonte</th><th>Valor</th></tr></thead><tbody>${cash.map(x=>`<tr><td>${new Date(x.occurred_at).toLocaleDateString('pt-BR')}</td><td>${esc(x.venture_slug)}</td><td>${esc(x.kind)}</td><td>${esc(x.source)}</td><td class="${x.kind==='revenue'?'money-positive':'money-negative'}">${money(x.amount_brl)}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">Nenhuma fonte financeira conectada. O Data Hub já aceita Woovi, Mercado Pago, Google Play, custos de IA/infra e lançamentos manuais.</div>'}</section>`;
}

async function renderGrowth(){
 setHeader('Growth Desk','ICP, prospecção, qualificação, abordagem, pipeline e fechamento para todo o portfólio.');const snap=await fetchSnapshot();const deals=snap.deals||[];const pipe=deals.filter(x=>!['won','lost'].includes(x.stage)).reduce((a,x)=>a+Number(x.value_brl||0),0);const won=deals.filter(x=>x.stage==='won').reduce((a,x)=>a+Number(x.value_brl||0),0);
 root.innerHTML=`<div class="grid3">${kpi('Oportunidades',deals.length,'pipeline consolidado')}${kpi('Pipeline aberto',money(pipe),'não realizado')}${kpi('Ganhos',money(won),'deals marcados como won')}</div><div class="grid2" style="margin-top:16px"><section class="panel"><p class="eyebrow">MODO PROSPECTOR</p><div class="hero-note">Selecionar uma venture → definir ICP → encontrar decisores → explicar o fit → gerar abordagem → registrar no pipeline.</div><div class="metric-strip" style="margin-top:16px"><span>ICP</span><span>OpenOutreach</span><span>Enrichment</span><span>Qualification</span><span>Outreach</span><span>CRM</span></div><div class="section-head"><h2>Motor compartilhado</h2><p>prioridade</p></div><div class="callout">OpenOutreach entra inicialmente como worker isolado. O moat fica na inteligência, memória e distribuição do MODO — não em copiar o projeto.</div></section><section class="panel"><div class="section-head" style="margin-top:0"><h2>Pipeline</h2><p>Data Hub</p></div>${deals.length?`<div class="list">${deals.slice(0,10).map(d=>`<div class="list-item"><div><b>${esc(d.company)}</b><div class="muted">${esc(d.venture_slug)} · ${esc(d.stage)}</div></div><span>${money(d.value_brl)}</span></div>`).join('')}</div>`:'<div class="empty">Ainda sem deals. A tabela <code>av_deals</code> já está pronta para MODO, CRM ou importação.</div>'}</section></div>`;
}

async function renderContracts(){setHeader('Contratos & Trust','Central da holding para criação, assinatura, audit trail e futura prova de integridade DocWallet.');const snap=await fetchSnapshot();const c=snap.contracts||[];root.innerHTML=`<section class="panel table-wrap">${c.length?`<table class="table"><thead><tr><th>Venture</th><th>Documento</th><th>Contraparte</th><th>Status</th><th>Atualizado</th></tr></thead><tbody>${c.map(x=>`<tr><td>${esc(x.venture_slug)}</td><td><b>${esc(x.title)}</b></td><td>${esc(x.counterparty)}</td><td><span class="pill">${esc(x.status)}</span></td><td>${new Date(x.updated_at).toLocaleDateString('pt-BR')}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">Nenhum contrato conectado. Evolução prevista: ContratoSmart → DocWallet Sign → audit trail → hash de integridade.</div>'}</section>`}

async function renderFounder(){setHeader('Founder AI','Cruza saúde, caixa, growth e telemetria para transformar dados em prioridades.');root.innerHTML='<div class="empty">Gerando briefing executivo…</div>';let brief='';try{const r=await apiFetch('/api/founder-brief');brief=(await r.json()).brief}catch{brief='Não foi possível gerar o briefing.'}root.innerHTML=`<section class="panel"><p class="eyebrow">BRIEF EXECUTIVO</p><div class="brief">${esc(brief)}</div></section><div class="callout" style="margin-top:16px">Nesta versão, o briefing é determinístico e auditável. A conexão com LLM entra depois que as fontes operacionais estiverem confiáveis — primeiro dados, depois narrativa.</div>`}

function renderConnectors(){setHeader('Conectores','Fontes que alimentam o AV OS. Segredos ficam nas funções serverless e nunca no navegador.');root.innerHTML=`<div class="grid2"><div class="connector"><div><strong>Supabase Data Hub</strong><p>Eventos, caixa, deals, contratos</p></div><span class="warn">CONFIGURÁVEL</span></div><div class="connector"><div><strong>GitHub</strong><p>Repos públicos já funcionam; token aumenta limite</p></div><span class="ok">ATIVO</span></div><div class="connector"><div><strong>Render</strong><p>Deploys, serviços e logs</p></div><span class="warn">PRÓXIMO</span></div><div class="connector"><div><strong>Netlify</strong><p>Sites e deploys</p></div><span class="warn">PRÓXIMO</span></div><div class="connector"><div><strong>Woovi / Pagamentos</strong><p>Caixa realizado por venture</p></div><span class="warn">PRÓXIMO</span></div><div class="connector"><div><strong>OpenStatus</strong><p>Uptime histórico e incidentes</p></div><span class="warn">PRÓXIMO</span></div></div><div class="section-head"><h2>Telemetria padrão</h2><p>POST /api/events</p></div><section class="panel"><div class="metric-strip"><span>user.created</span><span>trial.started</span><span>subscription.activated</span><span>payment.received</span><span>lead.created</span><span>deal.won</span><span>document.signed</span><span>deployment.completed</span><span>incident.opened</span></div></section>`}
